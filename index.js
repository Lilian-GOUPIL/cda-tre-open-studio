anime({
    targets: '.container-fluid',
    keyframes: [
        {opacity: 1}
    ],
    duration: 1000,
    easing: 'linear'
});

anime({
    targets: '.card',
    keyframes: [
        {opacity: 1}
    ],
    duration: 2000,
    easing: 'linear'
});